'use strict';

//import { expect } from 'chai';
//import { ImapToFilesystem } from '../src';

describe('ImapToFilesystem class', () => {
    /*it('should create an instance using its constructor', () => {
        const example: ImapToFilesystem = new ImapToFilesystem();
        expect(example, 'example should exist').to.exist;
        ImapToFilesystem;
    });*/
});
